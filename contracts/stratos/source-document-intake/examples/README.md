# Contract examples

Synthetic templates, not pre-provisioned production identities or sources. Replace source, project, governed-parent, actor, author, owner and gestor identifiers with actual authorized records and select the current source policy/revision. Do not run these templates unchanged against live data. `sample.txt` matches every example file hash and size. All examples use explicit TLP and complete knowledge-note profiles.
